\begin{diagram}[
  type=architecture,
  label={fig:sec03-lakehouse-layers},
  caption={The lakehouse stack from physical files to consumers; a table format and catalog add transactional metadata around plain files rather than replacing them.},
  source={Conceptual diagram.},
  description={Five stacked layers, top to bottom: Physical storage (Object Storage, Parquet or other file format); Table management (Table format, Catalog); Compute (Query or compute engine); Analytical surface (Warehouse, Lake, Lakehouse); Consumers (Consumers). The physical-storage layer is plain files; the table-management layer is the metadata that turns those files into a governed table.}
]
\begin{reportarchitecture}
  \layer{Physical storage}{Object Storage, Parquet or other file format}
  \layer{Table management}{Table format, Catalog}
  \layer{Compute}{Query or compute engine}
  \layer{Analytical surface}{Warehouse, Lake, Lakehouse}
  \layer{Consumers}{Consumers}
\end{reportarchitecture}
\end{diagram}
