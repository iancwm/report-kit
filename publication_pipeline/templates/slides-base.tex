% ReportKit slides base -- shared preamble/body-inclusion for every slides
% (Beamer) entrypoint. The slides counterpart of publication-template.tex
% (today's single monolithic paged entrypoint) once Phase A5 splits that
% file into paged-base.tex plus one entrypoint per publication type
% (decision D7). Added by Phase B (B1's file list) so A5 has a real file to
% consume; publication_pipeline/scripts/publication_build.py does not yet
% select or stage this file -- it still only knows the one hardcoded
% publication-template.tex (see the implementation plan's A5 section). Do
% not treat `reportkit build` as slides-capable until A5 lands.
%
% Consumes the same \RKPub... metadata macros write_metadata() already
% generates into metadata.tex for the paged renderer (title/author/date/
% license/etc. are renderer-neutral); no change there is needed for this
% file to work once A5 selects it. \RKFrontMatter is defined by the
% including entrypoint (e.g. presentation.tex), per D7's "entrypoint owns
% ... the publication-specific front matter hook" -- \input this file only
% after that macro is defined.
\input{metadata.tex}
\IfFileExists{links.tex}{\input{links.tex}}{}
\setreportkitleftheader{\RKPubLeftHeader}
\setreportkitfooter{\RKPubFooter}
\setreportkitversion{\RKPubVersion}
\setreportkitsubject{\RKPubSubject}
\setreportkitkeywords{\RKPubKeywords}
\title{\RKPubTitle}
\subtitle{\RKPubSubtitle}
\if\relax\detokenize\expandafter{\RKPubAuthor}\relax\author{}\else\author{\RKPubAuthor}\fi
\if\relax\detokenize\expandafter{\RKPubDate}\relax\date{}\else\date{\RKPubDate}\fi

\begin{document}
\RKFrontMatter
\input{body.tex}
\end{document}
